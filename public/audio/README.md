# Bundled audio files

Drop `.mp3` or `.ogg` files here, then register them in `src/config/tracks.ts`.

Expected files (optional until you add them):

- `rain.mp3`
- `fireplace.mp3`
- `piano.mp3`

If a file is missing, that track will fail quietly in the player until you add the file or remove the entry from the config.
